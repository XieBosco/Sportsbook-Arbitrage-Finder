[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sportsbook-arb-finder"
version = "0.1.0"
dependencies = [
    "websocket-client",
    "requests",
    "pyyaml",
    "fastapi",
    "uvicorn",
    "pytest"
]
